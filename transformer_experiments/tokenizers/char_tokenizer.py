# AUTOGENERATED! DO NOT EDIT! File to edit: ../../nbs/tokenizers/char-tokenizer.ipynb.

# %% auto 0
__all__ = ['SToI', 'IToS', 'EncodeFn', 'DecodeFn', 'create_character_tokenizer']

# %% ../../nbs/tokenizers/char-tokenizer.ipynb 4
from typing import Callable, Dict, Iterable, Tuple

# %% ../../nbs/tokenizers/char-tokenizer.ipynb 5
SToI = Dict[str, int]
IToS = Dict[int, str]
EncodeFn = Callable[[str], Iterable[int]]
DecodeFn = Callable[[Iterable[int]], str]


def create_character_tokenizer(
    text: str,
) -> Tuple[Iterable[str], int, SToI, IToS, EncodeFn, DecodeFn]:
    """Create a character tokenizer from text."""
    chars = sorted(list(set(text)))
    vocab_size = len(chars)
    stoi = {ch: i for i, ch in enumerate(chars)}
    itos = {i: ch for i, ch in enumerate(chars)}
    encode = lambda s: [stoi[c] for c in s]
    decode = lambda l: "".join([itos[i] for i in l])

    return chars, vocab_size, stoi, itos, encode, decode
